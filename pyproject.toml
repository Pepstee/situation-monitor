[project]
name = "situation-monitor"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["flask"]

[project.scripts]
edge = "situation_monitor.__main__:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"
